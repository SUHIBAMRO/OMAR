/* ==========================================================================
   config.js — كل ثوابت اللعبة وقواعد الضبط في مكان واحد
   ========================================================================== */
'use strict';

/* ------------------------- أنواع القطع العادية ------------------------- */
/* الترتيب مهم: رقم اللون = الفهرس هنا */
const PIECE_TYPES = [
  { key: 'crown',  name: 'التاج',   color: '#ffcc4d', dark: '#c97f05', light: '#ffe9a8' },
  { key: 'book',   name: 'الكتاب',  color: '#e8443f', dark: '#9e1f1b', light: '#ff8a85' },
  { key: 'shield', name: 'الدرع',   color: '#3b8ee2', dark: '#1a5391', light: '#8ec6ff' },
  { key: 'leaf',   name: 'الورقة',  color: '#41c153', dark: '#1d7a2c', light: '#93eaa0' },
  { key: 'gem',    name: 'الجوهرة', color: '#a465e8', dark: '#5f2a9c', light: '#d6b0ff' },
  { key: 'potion', name: 'الإكسير', color: '#ff8a3d', dark: '#b34a06', light: '#ffc08a' },
];

/* --------------------------- القطع الخاصة --------------------------- */
const SPECIAL = {
  NONE:    0,
  ROCKET_H: 1,   // صاروخ أفقي — يمسح الصف كاملاً
  ROCKET_V: 2,   // صاروخ عمودي — يمسح العمود كاملاً
  TNT:      3,   // قنبلة — تفجّر منطقة حولها
  BALL:     4,   // الكرة المضيئة — تمسح كل قطع لون واحد
};

/* -------------------------- عوائق الخلية -------------------------- */
/* box: يشغل الخلية بالكامل (لا توجد قطعة تحته)
   ice / chain: طبقة فوق القطعة تمنع تحريكها
   grass: طبقة تحت الخلية تُزال بأي انفجار أو مطابقة فوقها          */
const OBSTACLE = {
  BOX:   'box',
  ICE:   'ice',
  CHAIN: 'chain',
  GRASS: 'grass',
};

/* --------------------------- أنواع الأهداف --------------------------- */
const GOAL = {
  COLOR: 'color',   // اجمع N من لون معيّن
  BOX:   'box',     // حطّم N صندوقاً
  ICE:   'ice',     // اكسر N طبقة جليد
  CHAIN: 'chain',   // اكسر N سلسلة
  GRASS: 'grass',   // نظّف N بلاطة عشب
  ITEM:  'item',    // أنزل N من الأغراض (الصناديق الملكية) إلى الأسفل
  BOMB:  'bomb',    // أبطل مفعول N قنبلة موقوتة
};

const CFG = {
  /* ---------- اللوحة ---------- */
  board: {
    defaultRows: 9,
    defaultCols: 9,
    maxCols: 9,
    /* نصف قطر انفجار الـ TNT (مربّع منقوص الأركان) */
    tntRadius: 2,
    /* نصف قطر الانفجار الأكبر عند دمج TNT + TNT */
    tntBigRadius: 3,
    /* عرض الصاروخ المضاعف عند دمج صاروخ + TNT */
    rocketWide: 1,
  },

  /* ---------- التوقيتات (ميلي ثانية) ---------- */
  time: {
    swap: 170,        // تبديل قطعتين
    swapBack: 170,    // إرجاع تبديل فاشل
    pop: 230,         // تلاشي القطع المطابَقة
    fallStep: 88,     // سقوط خلية واحدة
    blastDelay: 55,   // تأخّر انتشار الانفجار بين خليّة وأخرى
    settleGap: 40,    // فاصل صغير بين المراحل
    hintDelay: 5000,  // بعد كم من السكون يظهر التلميح
    idleShuffle: 400, // قبل الخلط التلقائي
  },

  /* ---------- النقاط ---------- */
  score: {
    piece: 60,
    obstacleHit: 90,
    specialCreate: 150,
    specialBlast: 40,
    comboCreate: 400,
    /* مضاعف يزيد مع كل موجة تتابع (cascade) */
    cascadeMul: [1, 1.2, 1.5, 2, 2.5, 3, 4],
    /* حركة فائضة في نهاية المرحلة = صاروخ عشوائي + هذه النقاط */
    leftoverMove: 300,
  },

  /* ---------- الاقتصاد ---------- */
  economy: {
    startCoins: 500,
    winCoins: 25,
    firstWinBonus: 50,
    starCoins: 10,          // لكل نجمة جديدة
    extraMovesCost: 900,    // شراء 5 حركات إضافية
    extraMovesAmount: 5,
    boosterCost: {          // سعر كل معزّز داخل المرحلة
      hammer: 450,
      glove:  550,
      rocket: 700,
      tnt:    800,
      ball:   900,
      shuffle: 300,
    },
    preBoosterCost: {       // معزّزات ما قبل البدء
      rocket: 600,
      tnt:    700,
      ball:   900,
      moves:  500,
    },
  },

  /* ---------- القلوب / المحاولات ---------- */
  lives: {
    max: 5,
    refillMs: 20 * 60 * 1000,   // 20 دقيقة للقلب الواحد
    refillCost: 1200,           // ملء كل القلوب بالعملات
  },

  /* ---------- القنبلة الموقوتة ---------- */
  bomb: {
    defaultFuse: 9,     // عدد الحركات قبل الانفجار
    warnAt: 3,          // متى يبدأ التحذير الأحمر
  },

  /* ---------- المراحل المؤقّتة بالزمن ---------- */
  timed: {
    secPerPiece: 0.18,  // كل قطعة مُزالة تضيف ثوانيَ إلى المؤقّت
    secPerObstacle: 0.4,
    maxAdd: 12,         // أقصى ما تضيفه موجة واحدة
    warnAt: 15,         // متى يصير العدّاد أحمر
    leftoverSecScore: 120,
  },

  /* ---------- ضبط عام ---------- */
  hintEnabled: true,
  soundDefault: true,
  musicDefault: false,
};

/* أسماء المعزّزات بالعربية (للواجهة) */
const BOOSTER_INFO = {
  hammer:  { name: 'المطرقة',       desc: 'تحطّم أي قطعة أو عائق واحد.' },
  glove:   { name: 'القفّاز',        desc: 'يبدّل أي قطعتين متجاورتين دون شرط المطابقة.' },
  rocket:  { name: 'الصاروخ',       desc: 'يضع صاروخاً في مكان تختاره ويطلقه.' },
  tnt:     { name: 'القنبلة',        desc: 'تضع قنبلة في مكان تختاره وتفجّرها.' },
  ball:    { name: 'الكرة المضيئة', desc: 'تمسح كل قطع اللون الذي تختاره.' },
  shuffle: { name: 'الخلط',          desc: 'يعيد ترتيب كل القطع على اللوحة.' },
};
