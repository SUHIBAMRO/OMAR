# Timon's round-11 feedback, received 2026-09-14

In reply to the round-10 + provenance email
(`2026-09-14_final_reply_round10_and_provenance.md`). Verbatim.

---

Dear Omar,

this looks very nice now and I think Points 1–3 and 5 are now much clearer. Before we move to Point 4, I'd suggest a few final additions:

1. Please keep both break-even comparisons: the accuracy-matched comparison with NO at N=1401 versus the coarsest FEM mesh achieving comparable accuracy, and the resolution-matched comparison with NO and FEM both at N=1401. The latter should now be much more favourable to the NO after the inference optimization. In the paper we should discuss both

2. Please extend the N=1401 accuracy/QoI and break-even analysis, as far as possible, to all three constitutive models and both B1/B2. For the accuracy-matched comparison, please clarify which quantities are used to define the FEM resolution (L2, H1, energy, reactions, stress/peak stress, etc.), ideally in one table. At the moment, it is not fully clear to me what exactly makes N=11 the accuracy-matched FEM case. I understand that it cannot be in all QoIs and norms.

3. Can you please describe the protocol for the multi-resolution training more detailed. The new B1 Neo-Hookean checkpoint is trained at N=21, 33, 101, 201 and then evaluated at N=1401, correct? Please state how the different resolutions are sampled/weighted during training and why these resolutions were chosen.

4. For B1 Neo-Hookean, let's test one model trained directly at N=1401, if computationally feasible. This would be a useful ablation against the multi-resolution N=21/33/101/201 -> 1401 model in terms of training cost, memory, final accuracy/QoIs and inference time. The zero-shot N=1401 result should of course remain separate, because that is the actual resolution-generalization test.

Once this is finished, let's discuss Point 4 separately before you invest the GPU time into the new geometry. In brief, I think the notched ring is a good starting point, but the final example should include different geometries rather than only one fixed geometry. Most important is that the N for FEM should be large enough to see an advantage of the NO … hoping it can be accurate enough :).

Best regards,
Timon
